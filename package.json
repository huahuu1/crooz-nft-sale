{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "pint": "php vendor/bin/pint",
        "git": "npm run pint && git add . && git commit -m",
        "optimize": "php artisan optimize && php artisan config:clear && php artisan cache:clear && php artisan view:clear && php artisan route:clear && composer dump-autoload"
    },
    "devDependencies": {
        "axios": "^0.25",
        "laravel-vite-plugin": "^0.2.1",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^2.9.11"
    }
}
